#include "maze.h"

void    BFS(int x, int y)
{
    Q.push(make_pair(x, y));
    while (!Q.empty())
    {
        int xspx;
        int xspy;
        x = Q.front().first;
        y = Q.front().second;
        Quptd = 0;

        for (int sp = 0; sp < 4; sp++)
        {
            
        }
    }
}