#include <iostream>

// A for loop has a intialization, condition, statement and incrementing
// the intialized value.

int     main(void)
{
    for (int i = 0; i < 10; i++)
    {
        std::cout << "I am a number: " << i << std::endl;
    }
    return (0);
}
